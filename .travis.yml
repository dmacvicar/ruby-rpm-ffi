# Wake up travis!!
language: ruby
before_install:
  - sudo apt-get install rpm
  - sudo apt-get install -y -q fakeroot fakechroot
  - sudo apt-get install -y -q locales
  - sudo rm -rf /etc/locale.gen
  - sudo echo "en_US.UTF-8 UTF-8" > /etc/locale.gen
  - sudo echo "es_ES.UTF-8 UTF-8" > /etc/locale.gen
  - sudo /usr/sbin/locale-gen
#  - sudo bash -c "DEBIAN_FRONTEND=noninteractive dpkg-reconfigure locales"
rvm:
  - 1.9.3
script: fakechroot rake

